model:
  id_residual_weight: 0.5
  last_item_residual_weight: 0.5
  lambda_rank: 0.0
  use_multi_granularity_time: false
  use_modality_adaptive_time_weight: false

train:
  checkpoint_dir: checkpoints/ra_ctmid/toys_ra_without_mgt
