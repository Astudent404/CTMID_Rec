# Continuous-Time Multimodal Interest Dynamics for Sequential Recommendation

## 00 Research Canvas

### 原始 Idea
多模态信息的"时效性"不同——商品图片可能几个月不变，价格和促销标签可能每天变，新闻标题可能几小时失效，短视频封面/热点标签可能分钟级变化。现有多模态序列推荐很少显式建模这种**模态异步演化**。提出为每种模态学习一个连续时间动力学函数（ODE），同时维护用户侧兴趣 ODE 和 item 侧流行趋势 ODE，再用多粒度时间编码注入序列。

### 研究问题
**在多模态序列推荐中，如何显式建模不同模态信息的异质时间衰减速率与演化动力学，从而更准确地捕捉用户兴趣的时变性？**

### 初步创新假设
1. 不同模态具有本质不同的"信息半衰期"（image > text > price > trending tags），应当用独立的连续时间动力学函数分别建模。
2. 用户兴趣在不同模态维度上的演化速度也不同（视觉偏好稳定，价格敏感度随促销波动），需要模态感知的用户兴趣 ODE。
3. Item 的全局流行度在不同模态维度上的变化也是异步的（图片不变但价格骤降导致热度飙升），需要模态感知的 item 分布 ODE。

### 约束条件
- 需要在公开数据集上验证（Amazon Reviews 系列有时间戳和多模态信息）
- 计算开销需可控（ODE solver 的步数不能太多）
- 需要与现有多模态 SR 和时间感知 SR 方法进行公平对比

### 当前不确定点
- 公开数据集中模态时效性差异是否足够显著（Amazon 数据集中价格变化有限）
- ODE 求解的计算开销是否可接受
- 是否需要额外的模态时间尺度先验，还是完全从数据中学习

---

## 01 Idea Decomposition

### 任务
多模态序列推荐（Multi-Modal Sequential Recommendation）：给定用户历史交互序列及每个 item 的多模态信息（图像、文本、价格、类别等），预测用户下一个交互的 item。

### 问题（痛点）
1. **模态时效性被忽略**：现有方法（HM4SR、MTSTRec、M3SRec 等）将所有模态视为同步更新，忽略了不同模态信息的生命周期差异。
2. **时间建模粒度不足**：即使引入时间信息（如 HM4SR 的 Temporal MoE），也只是用时间嵌入做路由/加权，没有建模模态状态随时间的连续演化。
3. **用户兴趣与 item 分布的时间错配**：TGODE 虽然同时建模了用户兴趣和 item 分布的时间演化，但没有考虑多模态维度的异步性。

### 核心方法
**Modality-Specific Neural ODE (MS-ODE)**：为每种模态 $m$ 学习独立的连续时间动力学：

$$\frac{dh^{(m)}(t)}{dt} = f_m(h^{(m)}(t), \Delta t, \text{context})$$

其中：
- $h^{(m)}(t)$ 是模态 $m$ 在时间 $t$ 的隐状态
- $f_m$ 是模态特定的动力学函数（参数化为小型 MLP）
- context 包含跨模态信息和全局时间上下文

**双流架构**：
- **用户兴趣流**：每种模态维护独立的用户兴趣 ODE，捕捉用户在不同模态维度上的兴趣演化速度差异
- **Item 分布流**：每种模态维护独立的 item 流行度 ODE，捕捉 item 在不同模态维度上的热度变化

**多粒度时间编码**（借鉴 RoTE）：
- 将时间戳分解为多个粒度（秒、小时、天、周、月、季节）
- 使用旋转位置编码将多粒度时间信号注入序列编码
- 不同模态使用不同的时间粒度权重（图像侧重月/季节，价格侧重天/小时）

**模态间时间对齐融合**（借鉴 MTSTRec 的 TST 思想）：
- 在 ODE 演化后，使用时间对齐的共享 token 进行跨模态信息交换
- 确保不同速度演化的模态在同一时间点上的表示是一致的

### 训练目标
- 主任务：下一个 item 预测（cross-entropy loss）
- 辅助任务 1：模态时间尺度正则化——鼓励不同模态的 ODE 学到不同的演化速度
- 辅助任务 2：时间一致性对比学习——同一 item 在相近时间的表示应相似，远离时间的表示可以不同（尤其对快变模态）
- 辅助任务 3：跨模态预测——用慢变模态预测快变模态的变化趋势

### 数据需求
- 用户交互序列 + 精确时间戳
- Item 多模态信息：图像、文本描述、价格、类别
- 理想情况下需要 item 属性随时间变化的记录（价格变动历史）

### Evaluation
- 标准指标：Recall@K, NDCG@K, MRR@K (K=5,10,20)
- 时间感知评估：按时间间隔分组评估（短间隔 vs 长间隔）
- 模态贡献分析：消融不同模态的 ODE 组件
- 冷启动场景：新 item / 长时间未活跃用户

### 贡献声明
1. **首次提出模态异步演化的概念**：明确指出不同模态具有不同的信息半衰期，并将其形式化为模态特定的连续时间动力学。
2. **双流模态感知 ODE 框架**：同时建模用户兴趣和 item 分布在不同模态维度上的异步演化。
3. **多粒度时间编码与模态耦合**：将 RoTE 式的多粒度时间编码扩展到模态感知场景，不同模态自适应地关注不同时间尺度。
4. **广泛的实验验证**：在多个数据集上验证模态异步演化建模的有效性。

---

## 02 方法详细设计

### 2.1 问题形式化

设用户集合 $\mathcal{U}$，物品集合 $\mathcal{V}$，模态集合 $\mathcal{M} = \{id, img, txt, price, ...\}$。用户 $u$ 的交互序列为：

$$S_u = \{(v_1, t_1), (v_2, t_2), ..., (v_n, t_n)\}$$

每个物品 $v$ 具有多模态表示 $\{x_v^{(m)}\}_{m \in \mathcal{M}}$。目标是预测时间 $t_{n+1}$ 时用户的下一个交互。

### 2.2 模态特定初始表示

对每种模态使用预训练编码器提取初始特征：
- **ID**：可学习嵌入矩阵 $M_{id} \in \mathbb{R}^{|\mathcal{V}| \times d}$
- **Image**：冻结的 ViT 编码器 + 线性投影
- **Text**：冻结的 BERT/LLM 编码器 + 线性投影
- **Price**：标量归一化 + 可学习嵌入

### 2.3 模态感知连续时间动力学（核心模块）

#### 2.3.1 用户兴趣 ODE

对每种模态 $m$，用户在模态 $m$ 上的兴趣状态 $h_u^{(m)}(t)$ 满足：

$$\frac{dh_u^{(m)}(t)}{dt} = f_{\theta_m}^{user}\left(h_u^{(m)}(t), g^{(m)}(t), c_u(t)\right)$$

其中：
- $f_{\theta_m}^{user}$ 是模态 $m$ 特定的动力学网络（2层 MLP + tanh 激活）
- $g^{(m)}(t)$ 是模态 $m$ 的多粒度时间编码
- $c_u(t)$ 是跨模态上下文（其他模态状态的聚合）

**关键设计**：不同模态的动力学网络 $f_{\theta_m}$ 参数独立，允许学到不同的演化速度。通过初始化和正则化引导：
- 图像模态的动力学函数输出幅度较小（慢变）
- 价格/标签模态的动力学函数输出幅度较大（快变）

#### 2.3.2 Item 分布 ODE

类似地，item $v$ 在模态 $m$ 上的全局流行度状态：

$$\frac{de_v^{(m)}(t)}{dt} = f_{\phi_m}^{item}\left(e_v^{(m)}(t), g^{(m)}(t), \mathcal{N}_v(t)\right)$$

其中 $\mathcal{N}_v(t)$ 是 item $v$ 在时间 $t$ 附近的交互统计（反映外部因素如促销）。

#### 2.3.3 ODE 求解

使用 Runge-Kutta 4 阶方法（或自适应步长 Dormand-Prince）求解。为控制计算开销：
- 在训练时使用固定步长（2-4步）
- 在推理时可使用更多步数提升精度
- 对长时间间隔使用对数时间缩放

### 2.4 多粒度旋转时间编码（借鉴 RoTE）

将时间戳 $t$ 分解为多粒度表示 $(y, mo, d, h, min)$，对每种模态 $m$ 计算时间编码：

$$g^{(m)}(t) = \alpha_y^{(m)} \cdot \text{RoPE}(y, \text{base}_y) + \alpha_{mo}^{(m)} \cdot \text{RoPE}(mo, \text{base}_{mo}) + \alpha_d^{(m)} \cdot \text{RoPE}(d, \text{base}_d)$$

**模态自适应权重**：
- 图像模态：$\alpha_y^{img} > \alpha_{mo}^{img} > \alpha_d^{img}$（关注长期趋势）
- 价格模态：$\alpha_d^{price} > \alpha_{mo}^{price} > \alpha_y^{price}$（关注短期波动）
- 文本模态：介于两者之间

权重 $\alpha$ 可以是可学习参数，也可以用先验初始化后微调。

### 2.5 跨模态时间对齐融合

在 ODE 演化得到各模态在目标时间 $t_{n+1}$ 的表示后，使用改进的 TST 机制进行融合：

$$z_t^{sh} = \frac{1}{|\mathcal{M}|} \sum_{m \in \mathcal{M}} \text{Att}(h^{(m)}(t) \| z^{sh}_{t-1})$$

这确保了即使不同模态以不同速度演化，在融合时仍然是时间对齐的。

### 2.6 预测与损失

最终预测分数：

$$\hat{y}_v = \sum_{m \in \mathcal{M}} h_u^{(m)}(t_{n+1}) \cdot e_v^{(m)}(t_{n+1})$$

总损失：

$$\mathcal{L} = \mathcal{L}_{rec} + \lambda_1 \mathcal{L}_{tempo} + \lambda_2 \mathcal{L}_{contrast} + \lambda_3 \mathcal{L}_{cross}$$

其中：
- $\mathcal{L}_{rec}$：交叉熵推荐损失
- $\mathcal{L}_{tempo}$：时间尺度多样性正则（鼓励不同模态 ODE 的 Jacobian 范数不同）
- $\mathcal{L}_{contrast}$：时间感知对比学习
- $\mathcal{L}_{cross}$：跨模态时间预测损失

---

## 03 与现有工作的差异分析

| 维度 | TGODE | RoTE | HM4SR | MTSTRec | **Ours** |
|------|-------|------|-------|---------|----------|
| 多模态 | ✗ | ✗ | ✓ | ✓ | ✓ |
| 连续时间建模 | ✓ (ODE) | ✗ | ✗ | ✗ | ✓ (ODE) |
| 显式时间编码 | ✗ | ✓ (多粒度) | ✓ (间隔+绝对) | ✗ (仅位置) | ✓ (模态感知多粒度) |
| 模态异步演化 | ✗ | ✗ | ✗ | ✗ | ✓ |
| Item 分布建模 | ✓ | ✗ | ✗ | ✗ | ✓ (模态感知) |
| 用户兴趣动力学 | ✓ (单一) | ✗ | ✗ | ✗ | ✓ (模态特定) |

**核心差异总结**：
- vs TGODE：我们将 ODE 扩展到多模态场景，每种模态有独立动力学
- vs RoTE：我们不只是加时间编码，而是用 ODE 建模状态演化，时间编码是 ODE 的输入
- vs HM4SR：我们不只是用时间做路由，而是建模模态状态的连续演化
- vs MTSTRec：我们的时间对齐不只是位置对齐，而是考虑了模态演化速度差异

---

## 04 实验方案

### 4.1 数据集

| 数据集 | 理由 | 模态 |
|--------|------|------|
| Amazon Beauty (5-core) | 标准 benchmark，图像重要 | ID, Image, Text, Price, Category |
| Amazon Toys (5-core) | 标准 benchmark，季节性强 | ID, Image, Text, Price, Category |
| Amazon Sports (5-core) | 标准 benchmark，时间跨度大 | ID, Image, Text, Price, Category |
| Amazon Video Games | 价格波动大，促销频繁 | ID, Image, Text, Price, Category |

### 4.2 Baselines（分层）

**经典序列推荐**：
- GRU4Rec, SASRec, BERT4Rec

**时间感知序列推荐**：
- TiSASRec, RoTE-SASRec, FEARec, TiCoSeRec

**连续时间方法**：
- GNG-ODE, GDERec, TGODE

**多模态序列推荐**：
- UniSRec, MISSRec, M3SRec, IISAN, TedRec, HM4SR

**最接近的方法**（需重点对比）：
- TGODE（连续时间 + item 分布，但无多模态）
- HM4SR（多模态 + 时间 MoE，但无 ODE）

### 4.3 评估指标

- Recall@K, NDCG@K (K=5, 10, 20)
- 全排序评估（无负采样）
- 5 个随机种子取平均
- 配对 t 检验验证显著性

### 4.4 消融实验

| 变体 | 说明 |
|------|------|
| w/o MS-ODE | 移除模态特定 ODE，用共享 ODE |
| w/o Item-ODE | 移除 item 分布 ODE |
| w/o Multi-Granularity | 用单一时间编码替代多粒度 |
| w/o Modal-Adaptive-Time | 所有模态共享相同时间权重 |
| w/o Cross-Modal-Fusion | 移除跨模态时间对齐融合 |
| w/o Tempo-Reg | 移除时间尺度多样性正则 |
| Single-Speed | 所有模态使用相同演化速度 |

### 4.5 深入分析

1. **模态演化速度可视化**：展示不同模态 ODE 学到的 Jacobian 范数分布，验证确实学到了不同速度
2. **时间间隔分组评估**：按用户交互间隔分组（<1天, 1-7天, 7-30天, >30天），验证长间隔场景的提升
3. **促销/事件场景分析**：选取价格变动大的 item，验证模型能捕捉价格驱动的兴趣变化
4. **效率分析**：对比 ODE 步数与性能/时间的 trade-off
5. **Case Study**：展示具体用户序列中不同模态兴趣的演化轨迹

### 4.6 预期结果

- **主指标**：在 NDCG@10 上相比最强 baseline（HM4SR 或 TGODE）提升 3-8%
- **长间隔场景**：提升更显著（10-15%），因为 ODE 能更好地建模长时间演化
- **价格敏感场景**：在价格波动大的 item 上提升明显
- **代价**：训练时间增加 30-50%（ODE 求解开销），推理时间增加 20-30%

---

## 05 代码实现架构

```
CTMID/
├── configs/
│   ├── default.yaml
│   ├── beauty.yaml
│   ├── toys.yaml
│   └── sports.yaml
├── datasets/
│   ├── base_dataset.py
│   ├── multimodal_seq_dataset.py
│   └── data_utils.py
├── models/
│   ├── ctmid.py                 # 主模型
│   ├── modality_ode.py          # 模态特定 ODE 模块
│   ├── item_distribution_ode.py # Item 分布 ODE
│   ├── time_encoding.py         # 多粒度旋转时间编码
│   ├── cross_modal_fusion.py    # 跨模态时间对齐融合
│   └── feature_extractors.py    # 多模态特征提取器
├── modules/
│   ├── ode_solver.py            # ODE 求解器
│   ├── attention.py             # 注意力模块
│   └── losses.py                # 损失函数
├── trainers/
│   ├── trainer.py
│   └── evaluator.py
├── baselines/
│   └── ...
├── scripts/
│   ├── train.sh
│   ├── eval.sh
│   └── preprocess.sh
└── main.py
```

---

## 06 潜在风险与应对

| 风险 | 应对策略 |
|------|----------|
| ODE 计算开销过大 | 使用固定步长 + 伴随方法节省内存；限制 ODE 步数为 2-4 |
| 公开数据集模态时效性差异不明显 | 构造合成实验验证；选择价格变动大的数据集子集 |
| 模态 ODE 退化为相同速度 | 时间尺度多样性正则 + 初始化策略 |
| 与 HM4SR 差异不够大 | 强调 ODE 的连续演化 vs MoE 的离散路由本质区别 |
| 训练不稳定 | 梯度裁剪 + ODE 正则化（限制 Lipschitz 常数）|

---

## 07 相似性矩阵（Similarity Matrix）

基于检索结果，以下是与本 idea 最相关的已有工作：

| 论文 | 年份/来源 | 核心方法 | 相似点 | 差异点 | 相似等级 |
|------|-----------|----------|--------|--------|----------|
| **TGODE** (Fu et al.) | KDD 2025 | 用户时间图 + Item演化图 + Graph Neural ODE + 扩散生成器 | 同时建模用户兴趣和item分布的时间演化；使用ODE建模连续时间 | 不涉及多模态；ODE是单一的，不区分模态；用扩散模型做数据增强 | S3 |
| **HM4SR** (Zhang et al.) | WWW 2025 | 层次化MoE + 显式时间嵌入（间隔+绝对时间）+ 多模态 | 多模态+显式时间信息；用时间嵌入做路由 | 无ODE/连续时间建模；时间只做路由权重，不建模状态演化；无模态异步概念 | S2 |
| **RoTE** (Zhang et al.) | SIGIR 2026 | 多粒度旋转时间编码（年/月/日）+ plug-and-play | 多粒度时间编码思想一致 | 不涉及多模态；无ODE；只是时间编码模块，不建模动力学 | S2 |
| **MTSTRec** (Hong et al.) | ICML 2025 | 时间对齐共享Token + 多模态融合 | 多模态时间对齐融合 | 无连续时间建模；"时间对齐"指位置对齐而非动力学对齐；无模态异步演化 | S2 |
| **Stein-Guided Entropic Flow** (Wang et al.) | KDD 2025 | Stein变分 + 熵流 + 多模态序列推荐 | 多模态+时间动力学建模 | 用Stein变分而非ODE；关注分布对齐而非模态异步演化；无模态特定时间尺度 | S2 |
| **SS4Rec** (Continuous-Time SR with SSM) | 2025 | 时间感知状态空间模型 + 关系感知SSM | 连续时间建模用户兴趣 | 不涉及多模态；用SSM而非ODE；无模态异步概念 | S2 |
| **GDERec** (Qin et al.) | TKDE 2024 | Graph ODE + 自回归交替训练 | 连续时间ODE建模用户偏好演化 | 不涉及多模态；单一ODE；无模态异步 | S2 |
| **Transfer-Efficient MM-SR with State Space Duality** | 2025 | 状态空间对偶 + 多模态迁移 | 多模态序列推荐 + 状态空间建模 | 关注迁移效率而非时间动力学；无模态异步演化 | S1 |

### 风险判断

**无 S4 级别的重合**。最接近的是 TGODE（S3），但它完全不涉及多模态，我们的核心创新——"模态异步演化"——在所有检索到的工作中均未被提出。

---

## 08 新颖性评估（Novelty Assessment）

### 当前结论
**可以进入实验设计阶段**。核心创新点"模态异步演化动力学"在现有文献中没有被直接提出或解决。

### 已覆盖范围
- 连续时间ODE用于序列推荐（TGODE, GDERec, GNG-ODE）→ 但都是单模态
- 多模态序列推荐中引入时间信息（HM4SR）→ 但只是离散的时间嵌入做路由
- 多粒度时间编码（RoTE）→ 但不涉及多模态
- 多模态时间对齐融合（MTSTRec）→ 但是位置对齐，非动力学对齐

### 未覆盖范围（我们的创新空间）
1. ✅ **模态异步演化的概念**：没有工作明确提出不同模态有不同"信息半衰期"
2. ✅ **模态特定ODE**：没有工作为每种模态学习独立的连续时间动力学
3. ✅ **模态感知多粒度时间编码**：RoTE的多粒度编码没有与模态耦合
4. ✅ **双流模态感知ODE**：TGODE的双流（用户+item）没有扩展到多模态维度

### 可以保留的贡献
- 全部四个创新点均可保留

### 必须重构的贡献
- 无需重构

### 推荐路线
**直接进入实验设计**（当前版本 V0 即可）

---

## 09 一句话总结

**CTMID 是首个显式建模多模态信息异步时间演化的序列推荐框架，通过模态特定的神经 ODE 捕捉不同模态的差异化生命周期，结合多粒度时间编码和跨模态时间对齐融合，在用户兴趣建模中实现了"快变模态快响应、慢变模态稳记忆"的效果。**
